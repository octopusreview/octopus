{
  "name": "@octopus/package-analyzer",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "devDependencies": {
    "@octopus/tsconfig": "workspace:*",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
